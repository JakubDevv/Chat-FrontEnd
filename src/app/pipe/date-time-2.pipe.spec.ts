import { DateTime2Pipe } from './date-time-2.pipe';

describe('DateTime2Pipe', () => {
  it('create an instance', () => {
    const pipe = new DateTime2Pipe();
    expect(pipe).toBeTruthy();
  });
});
